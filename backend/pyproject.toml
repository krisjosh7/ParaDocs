[tool.pytest.ini_options]
testpaths = ["tests", "rag/tests"]
markers = [
    "integration: full RAG path (HTTP + Chroma + disk); no Ollama",
    "groq: requires GROQ_API_KEY; skipped if unset",
]

[tool.coverage.run]
source = ["."]
omit = [
    "*/venv/*",
    "*/tests/*",
    "*/rag/tests/*",
    "*/__pycache__/*",
]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "if __name__ == .__main__.:",
]
