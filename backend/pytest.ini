[pytest]
asyncio_mode = auto
testpaths = tests rag/tests
markers =
    integration: full stack or external services (RAG HTTP+Chroma+disk, research APIs, etc.)
    gemini: requires GEMINI_API_KEY; skipped if unset
